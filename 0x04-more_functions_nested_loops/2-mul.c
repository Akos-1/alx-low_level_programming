#include "main.h"
#include <stdio.h>

/**
 * mul - multiplication of two integers
 * @a: first integer
 * @b: second integer
 * Return: 0
 */

int mul(int a, int b)
{
	int result = a * b;

	return (result);
}
